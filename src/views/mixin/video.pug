mixin video(info)

    div 
        h4=info.title 
        ul 
            li #{info.rating}
            li #{info.comment} comment
            li posted #{info.createdAt}
            li #{info.views} view